export enum DealerMode {
  dealer_local,
  dealer_remote,
}

export enum UserMode {
  player,
}

export enum CameraType {
  user = "user",
  table = "table",
}

export enum LayoutType {
  split = "split",
  pip = "pip",
}
